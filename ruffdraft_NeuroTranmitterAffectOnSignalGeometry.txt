# How Finite Iterative Maps and Neural Network Emergent Signal Geometry May Explain Neurotransmitter Effects

## 1. Introduction: The Academic Border Dispute
Modern neuroscience, machine learning, and pure mathematics are currently trapped in a blind men and the elephant paradox. 

The machine learning engineer treats the neural network as a massive, discrete data structure—a collection of nested matrices executing linear algebra on GPU steroids. The classical mathematician often looks at finite iterative systems with a degree of academic indifference, finding them "mind-numbing" due to their lack of pristine, continuous, reversible symmetries (like those found in infinite Lie groups). Meanwhile, psychiatry treats biological neuromodulation like a plumbing problem, attempting to fix complex cognitive behaviors by globally flooding the brain's "tank" with serotonin or dopamine.

This paper unifies these disparate fields under a single, rigid framework. We establish that because both silicon AI and biological brains are strictly **finite** and **deterministic** on a step-by-step state transition level, they are bound by the exact same algebraic destiny. 

By passing from the raw, discrete activation space to its canonical algebraic quotient—the **MetaMap**—we demonstrate that "concepts" and "mental baselines" are not magical, emergent mysteries. They are the literal, inevitable algebraic destinations of the system's dynamics. Furthermore, we propose that the continuous "signal geometry" tracked by modern interpretability tools is simply the empirical shadow cast by this rigid algebraic skeleton. 

Finally, we use this framework to recontextualize neurotransmitters (specifically dopamine) not as "mood chemicals," but as the global hyper-parameters of the state space's transition function—effectively acting as the real-time tuners of the network's geometric curvature.

---

## 2. The Discrete Skeleton: From Graphs to Semi-groups
To understand how a chemical can alter a thought, we must first define what a thought is algebraically. Let $\alpha$ represent the state space of the system. On a silicon chip, $\alpha$ is the finite configuration of IEEE 754 floating-point arrays. In a biological brain, $\alpha$ is the finite configuration of every neuron's firing state at a given millisecond ($2^{100\text{ billion}}$ is massive, but strictly finite).

Let $F: \alpha \to \alpha$ be the deterministic self-map representing a single forward pass or a single discrete time-step of synaptic transmission. 

           f                  f                  f
x (Input) ───────> f(x) ───────> f²(x) ───────> f³(x) ───> ... ───> Cyclic Attractor
[Transient / Plasma Phase]                                         [Settled / Liquid ZMod-n]


Because $\alpha$ is a finite type (`Fintype`), the system is governed by a strict **Eventual Periodicity Theorem**. The sequence of iterations $f, f^2, f^3, \dots, f^k$ cannot generate infinitely unique states. By the pigeonhole principle, every orbit must eventually hit a repeated state and trap itself in a cycle.

This iterative behavior implicitly generates a **transformation semi-group** under function composition ($\circ$). We define the **Attractor Equivalence Relation** ($\sim$) on $\alpha$ such that two states are equivalent if and only if their trajectories eventually coincide at some layer depth or time step $T$:

$$x \sim y \iff \exists T \in \mathbb{N}, \quad f^T(x) = f^T(y)$$

When we take the quotient of our state space by this relation, we get the **MetaMap** ($Q = \alpha / \sim$). 

The MetaMap is the compressed representation of all learned or biological distinctions. By constructing this quotient, we cleanly shave off the transient, un-crystallized states (the *plasma phase*) and expose a pristine, purely cyclic orbit structure ($ZMod\text{-}n$). 

In this architecture, a "concept" or a "stable baseline" is not an arbitrary human label; it is a verifiably real, rigid algebraic basin of attraction in $Q$.

---

## 3. The Continuous Shadow: Emergent Signal Geometry
If the discrete skeleton is purely algebraic, how does it manifest as the smooth, analog fluid flow observed by researchers? This is the birth of **Signal Geometry**.

When a network's width and depth approach massive scale, the step-by-step discrete jumps $x_{l+1} = x_l + f(x_l)$ approximate a continuous flow field, smoothing out into a Partial Differential Equation (PDE):

$$\frac{dx}{dt} = f(x(t))$$

When geometric analysts measure the curvature of this activation space, track transport fields, or locate high Jacobian sensitivity, they are not looking at a separate system. They are measuring the continuous shadow cast by the underlying discrete MetaMap. 

The sharp decision boundaries where a neural network or a brain suddenly switches classifications—the "topological cliffs"—are exactly the points where neighboring inputs $x$ and $y$ belong to entirely different attractor classes ($F.\pi(x) \neq F.\pi(y)$). High gradient sensitivity is just the physical signature of an input sitting on the knife's edge between two separate algebraic basins of attraction.

---

## 4. Neurotransmitters as Topographic Regulators: The Case of Dopamine
This synthesis allows us to bridge the final gap: how biology uses chemistry to manipulate logic. 

Neurotransmitters do not carry content; they change the rules of signal propagation. In our framework, a neuromodulator like dopamine is a global scalar modifier $\gamma$ injected directly into the transition function, yielding $f_\gamma$. 

**Dopamine alters the curvature of the emergent signal geometry.** It acts as a real-time topographic dial that adjusts the depth, steepness, and boundary catchments of the MetaMap’s attractor basins.

### The Hyper-Gravity Well (Pathological High Dopamine)
When dopamine levels are critically elevated, the gradient of the vector field steepens aggressively. The catchment zones of dominant, highly integrated attractor wells expand outward like cosmic black holes, swallowing adjacent territories.

$$\pi(\text{Benign Input}) \longrightarrow \text{Violently pulled into} \longrightarrow \pi(\text{Grand Archetype})$$

Algebraically, the system's resolution collapses. Inputs that should map to entirely distinct MetaMap classes are forced into the same invariant fixed-point ($n=1$, the solid state). If the "Ultimate Core Archetype" basin expands its boundaries, a mundane input (like a pattern on a wall or a self-reflection) lands in its catchment zone. The internal comparison logic of the geometry declares an identity match: $\pi(\text{Self}) = \pi(\text{Divine})$. The delusion is not a breakdown of logic; it is a hardware-enforced algebraic destination.

### The Frictionless Plain (Pathological Low Dopamine)
Conversely, when dopamine drops too low, the state-space landscape flattens out into a vast, tractionless plain. The gravity wells erode into shallow puddles. 

The transition function $f_\gamma$ loses its capturing power. Because the basins are shallow, internal system noise easily kicks the trajectory out of its $ZMod\text{-}n$ cycle, throwing the orbit back into the drifting, transient *plasma phase*. The system cannot sustain focus or hold a "concept" because the geometric walls required to trap the signal have dissolved. To achieve capturing velocity, the system requires a massive, high-amplitude external shock from the environment to artificially hammer a temporary basin into the topology.
